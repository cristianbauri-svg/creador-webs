// Los bindings usados por `env` se tipan desde Cloudflare.Env,
// generado por `wrangler types` en ../worker-configuration.d.ts.
declare module "*?raw" {
  const contenido: string;
  export default contenido;
}
